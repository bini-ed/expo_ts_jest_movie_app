import { StyleSheet, Dimensions } from "react-native";

export const styles = StyleSheet.create({
  container: {
    margin: 5,
  },
  image: {
    borderRadius: 10,
  },
  title: {
    color: "white",
    textAlign: "center",
  },
});

import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    width: 250,
    height: 300,
    borderRadius: 20,
    resizeMode: "contain",
  },
  container: { margin: 5 },
  title: {
    color: "white",
  },
});
